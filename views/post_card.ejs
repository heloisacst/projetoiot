<div class="card" style="width: 18rem; margin: 45px;">
  <div class="card-body">
    <h5 class="card-title"><%= post.name %></h5>
    <p class="card-text"> <%= post.body %> </p>
    <a href="<%= `/posts/${post.id}` %>" class="btn btn-primary">Clique aqui</a>
  </div>
</div>